# UIPositions — single source of truth for every UI element's screen position.
# Edit a value here, it moves everywhere. All coords are pixels from top-left (0,0).
# Screen is 1280 × 720.
extends Node

# ── Minimap ──────────────────────────────────────────────────────────────────
const MINIMAP       := Vector2(1026, 12)   # top-left corner of the minimap frame

# ── Add new elements below as the game grows ─────────────────────────────────
# const HEALTH_BAR  := Vector2(14, 14)
# const HOTBAR      := Vector2(490, 688)
# const DIALOGUE    := Vector2(160, 560)
